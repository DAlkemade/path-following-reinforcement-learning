from setuptools import setup

setup(name='path_following_reinforcement_learning',
      version='0.0.1',
      install_requires=['gym', 'tensorflow'],  # And any other dependencies foo needs
      author="Daan Alkemade",
      )
